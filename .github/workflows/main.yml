name: Simple CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Hello, GitHub Actions!"

      - name: Show Node Version
        run: node -v
